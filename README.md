# 🀄 Mahjong Evaluator (配牌評価プラットフォーム)

「この配牌、何点？」
麻雀の配牌（最初の14枚）の価値を、ユーザーの集合知によって可視化・数値化するWebアプリケーションです。
自身の感覚が他者と比べて「強気」なのか「弱気」なのか、偏差値や分布グラフを用いて統計的に分析できます。

## 📖 概要

麻雀において「配牌の良し悪し」は主観に依存しがちです。このアプリでは、スライダーによる直感的な投票システムと、バックエンドによるリアルタイム統計処理（平均、標準偏差、偏差値算出）を組み合わせることで、配牌の価値を客観的なデータとして定義します。

## ✨ 主な機能

### ユーザー向け機能
- **LP & 認証**:
  - モダンなランディングページ
  - ユーザー登録 / ログイン機能（JWT/Session管理）
- **投票システム**:
  - 配牌画像を見ながら 0〜100点で評価
  - 直感的なスライダーUI
- **統計分析結果**:
  - 投票直後に集計結果を表示
  - **平均点** と **標準偏差($\sigma$)** の算出
  - ユーザーの入力値に対する **偏差値($T$-score)** 判定
  - 投票分布のヒストグラム表示 (Recharts)

### 管理者向け機能
- **ダッシュボード**:
  - 全問題の管理（一覧表示・削除）
- **問題作成エディタ**:
  - 34種類の牌パレットからクリックで選択
  - 手牌13枚＋ツモ1枚＋ドラ表示牌を直感的に作成可能

## 🛠 使用技術 (Tech Stack)

### Frontend
- **Framework**: Next.js 14 (App Router)
- **Language**: TypeScript
- **Styling**: Tailwind CSS
- **Visualization**: Recharts (グラフ描画)
- **State Management**: React Hooks (useState, useEffect, Context)

### Backend
- **Language**: Go (Golang)
- **Framework**: Standard `net/http` (with minimal dependencies)
- **ORM**: GORM
- **Architecture**: REST API (MVC Pattern)

### Infrastructure / Database
- **Database**: PostgreSQL
- **Container**: Docker / Docker Compose

## 📂 ディレクトリ構成

```text
portfolio-root/
├── backend/            # Go API Server
│   ├── controllers/    # Request Handlers (Auth, Vote, Admin)
│   ├── models/         # DB Schemas & JSON Structs
│   ├── database/       # DB Connection & Migration
│   └── main.go         # Routing & Entry point
│
├── frontend/           # Next.js Client
│   ├── src/app/
│   │   ├── admin/      # Admin Dashboard & Editor
│   │   ├── problems/   # User Vote & Result Pages
│   │   ├── login/      # Auth Pages
│   │   └── ...
│   ├── src/components/ # Shared Components (Header, etc.)
│   └── public/tiles/   # Mahjong Tile Assets
│
└── docker-compose.yml  # DB Infrastructure
